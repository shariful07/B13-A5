1️⃣ What is the difference between var, let, and const?

Ans: var is a global Scope,
      let is a block Scope,
      const is block Scope,
      
2️⃣ What is the spread operator (...)?

Ans: To separate all the values ​​inside an array or object.

3️⃣ What is the difference between map(), filter(), and forEach()?

  Ans: Map do return new array, filter do return new array based on condition, forEach does not return
  
4️⃣ What is an arrow function?

  Ans: arrow function is callback
  
5️⃣ What are template literals?

Ans: Backticks are used and can be used to easily insert variables and write multi-line strings.
